export const ValidationMessages = {
    required: "You need to specify a $property",
    minLength: "The length of $property $value is too short, the minimum length is $constraint1",
    maxLength: "The length of $property $value is too long, the maximum length is $constraint1",
    enum: "You need to specify one of the already existing status"
};